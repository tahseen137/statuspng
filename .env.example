# Database
# Local development uses SQLite at ./data/statuspng.db
# Production should use Vercel Postgres, Turso, or Neon
DATABASE_URL="file:./data/statuspng.db"

# OpenAI API (for AI incident reports)
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-..."

# Optional: Email alerts (SMTP configuration)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASSWORD="your-app-password"
SMTP_FROM="noreply@statuspng.com"

# Optional: SMS alerts (Twilio)
TWILIO_ACCOUNT_SID="your-twilio-sid"
TWILIO_AUTH_TOKEN="your-twilio-token"
TWILIO_PHONE_NUMBER="+1234567890"

# Optional: Production database (Turso)
# Sign up at https://turso.tech and create a database
# TURSO_DATABASE_URL="libsql://your-db.turso.io"
# TURSO_AUTH_TOKEN="your-auth-token"

# Optional: Production database (Vercel Postgres)
# Automatically set by Vercel when you add Postgres storage
# POSTGRES_URL="postgres://..."
